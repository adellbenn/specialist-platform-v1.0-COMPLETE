-- ═══════════════════════════════════════════════════════════
-- تهيئة قاعدة البيانات — PostgreSQL
-- منصة إدارة أدوار الأخصائيين
--
-- ملاحظة مهمة: هذا الملف يُشغّل مرة واحدة فقط عند إنشاء
-- مجلد البيانات لأول مرة (postgres docker-entrypoint).
-- يتم إنشاء الجداول والبيانات الأساسية عبر TypeORM migrations
-- (خدمة `migrate` في docker-compose.prod.yml)، لذا لا تُضف
-- أي جداول أو بيانات بذرة هنا — ستتعارض مع المخطط الفعلي.
-- ═══════════════════════════════════════════════════════════

-- تفعيل امتداد UUID (مطلوب لتوليد UUID في migrations)
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- للبحث النصي السريع
CREATE EXTENSION IF NOT EXISTS "pg_trgm";
